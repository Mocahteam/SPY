using UnityEngine;

// This component is just a flag component to notify systems that editable scripts have been copied to executable panels
public class ExecutablePanelReady : MonoBehaviour {
	// Advice: FYFY component aims to contain only public members (according to Entity-Component-System paradigm).
}
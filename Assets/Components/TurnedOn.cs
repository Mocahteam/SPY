using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(Activable))]
public class TurnedOn : MonoBehaviour
{
    // A simple flag component to know if an activable GO is turnedOn or not
}
